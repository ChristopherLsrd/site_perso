import React, { useContext } from 'react';
import Navigation from '../Components/Navigation';


const School = () => {
    return (
        <div>

            <Navigation />

        </div>
    );
};

export default School;